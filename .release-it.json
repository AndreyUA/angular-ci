{
  "git": {
    "requireBranch": "main",
    "commitMessage": "chore: release v${version}"
  },
  "hooks": {
    "before:init": ["git pull origin main"],
    "after:bump": "npm run changelog-update && git add . && git commit -m 'updated changelog file'"
  }
}
